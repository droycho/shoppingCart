<li>{{item.title}}<br>{{item.cost}}<br>
{{#if isImageShowing}}
  <p><img src={{item.image}} alt={{item.description}} {{action 'imageHide'}}></p>
{{else}}
    <button {{action 'imageShow'}}>Show Image</button>
  {{/if}}
</li>
